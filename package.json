{
  "name": "@openlab/catalyst-trello-scraper",
  "private": true,
  "version": "0.4.0",
  "description": "Scrapes a Trello list, parses label-relationships and stores into redis",
  "main": "src/index.js",
  "types": "dist/index.d.ts",
  "repository": "unplatform/catalyst-trello-scraper",
  "author": "Rob Anderson (https://r0b.io)",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "lint": "npx tsc --noEmit",
    "coverage": "jest --coverage",
    "preversion": "npm run lint && npm test -s",
    "prettier": "prettier --write '**/*.{js,ts,tsx,json,css,md}'",
    "start": "node -r dotenv/config dist/cli.js",
    "test": "jest",
    "dev": "NODE_ENV=development ts-node -r dotenv/config src/cli.ts",
    "redis": "docker run -it --rm --name dev_redis -p 6379:6379 redis:4-alpine",
    "redis-cli": "docker exec -it dev_redis redis-cli",
    "gen-readme-toc": "npx @robb_j/md-toc -i"
  },
  "keywords": [
    "not-equal-catalyst",
    "trello",
    "scraper",
    "redis"
  ],
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "chalk": "^2.4.2",
    "commander": "^2.19.0",
    "dotenv": "^6.2.0",
    "format-ms": "^1.2.1",
    "node-cron": "^2.0.3",
    "redis": "^2.8.0",
    "valid-env": "^0.1.3"
  },
  "devDependencies": {
    "@robb_j/md-toc": "^0.2.2",
    "@types/jest": "^24.0.6",
    "@types/node": "^11.9.4",
    "@types/node-cron": "^2.0.1",
    "@types/redis": "^2.8.10",
    "husky": "^1.3.1",
    "jest": "^24.1.0",
    "lint-staged": "^8.1.4",
    "prettier": "1.16.4",
    "ts-jest": "^23.10.5",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  }
}
